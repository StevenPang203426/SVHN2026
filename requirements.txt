# ============================================================
#  SVHN 街景字符识别 — Python 依赖
# ============================================================

# ── 核心框架 ──
torch>=2.0.0
torchvision>=0.15.0
torchsummary>=1.5.1

# ── 数据处理 ──
numpy>=1.24.0
pandas>=2.0.0
Pillow>=9.5.0

# ── 配置管理 ──
pyyaml>=6.0

# ── 训练辅助 ──
tqdm>=4.65.0
matplotlib>=3.7.0

# ── 网络 / IO ──
requests>=2.28.0

# ── 因果增强（可选，不装也能跑分类模型） ──
albumentations>=2.0.0
opencv-python>=4.8.0

# ── 实验追踪（可选，不装也能跑） ──
wandb>=0.15.0

# ── YOLO 目标检测方案（可选） ──
# ultralytics>=8.0.0
